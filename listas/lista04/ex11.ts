// 11 · Tudo em MAIÚSCULO
// Use .map com .toUpperCase() pra deixar todos os nomes em maiúsculas.

let listaNo = ["Helena", " Gabriele", " Felipe", " Oscar", " Mariana"];
let listMai = listaNo.map(n => n.toUpperCase())

alert(`${listMai}`);