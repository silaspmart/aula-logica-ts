// 07 · Área do retângulo
// Crie area(base, altura) que retorna base × altura. Leia os dois e mostre.

let baseRe = Number(prompt("Valor da base:"));
let altRe = Number(prompt("Valor da base:"));

function areaRet(a,b) {
    return a*b
}

alert(`Área do retângulo: ${areaRet(baseRe, altRe)}`)